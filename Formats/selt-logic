constants:  T (true), F (false),

prefixes:   [] (always), <> (eventually), - (logic negation)
         
            /\ (and), \/ (or), of precedence 2
            <=, >=, =,, lt, ge, gt, of precedence 3
            +, of precedence 4
            *, of precedence 5

