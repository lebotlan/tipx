(* UTF8 base functions
 *
 * Similar to Text, but simpler, and portable since only pure OCaml (Text calls C-bindings) 
 *)


(* Performance note: single ascii characters are allocated only once. *)



